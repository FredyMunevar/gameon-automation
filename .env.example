# Copia este archivo a .env y rellena tus credenciales.
#   cp .env.example .env   &&   nano .env
# El archivo .env está en .gitignore: NO se versiona.

# API key de Firebase (del APK, no es secreta) — ya rellenada:
export FIREBASE_API_KEY='AIzaSyA0XKQKb4RyQQ0UtTbi7K9Ztzccw3rbd8g'

# Tu cuenta de GameOn:
export GAMEON_EMAIL='tu@correo.com'
export GAMEON_PASSWORD='tu_contraseña'

# Lo sacarás del --probe (déjalo vacío por ahora):
export GAMEON_GROUP_ID=''
